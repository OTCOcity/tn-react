import { DOM} from 'react';

const Preload = () => (
  DOM.div(
    {
      className: 'sk-fading-circle'
    },
    DOM.div({className: 'sk-circle1 sk-circle'}),
    DOM.div({className: 'sk-circle2 sk-circle'}),
    DOM.div({className: 'sk-circle3 sk-circle'}),
    DOM.div({className: 'sk-circle4 sk-circle'}),
    DOM.div({className: 'sk-circle5 sk-circle'}),
    DOM.div({className: 'sk-circle6 sk-circle'}),
    DOM.div({className: 'sk-circle7 sk-circle'}),
    DOM.div({className: 'sk-circle8 sk-circle'}),
    DOM.div({className: 'sk-circle9 sk-circle'}),
    DOM.div({className: 'sk-circle10 sk-circle'}),
    DOM.div({className: 'sk-circle11 sk-circle'}),
    DOM.div({className: 'sk-circle12 sk-circle'})
  )
);

export default Preload;

